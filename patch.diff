-  let out = cx
-      .try_catch(|cx| reflect_construct(cx, ctor, &[src_v, flags_v]))
-      .ok()
-      .flatten();
+  let out = cx
+      .try_catch(|cx| reflect_construct(cx, ctor, &[src_v, flags_v]))
+      .ok();

-  let out = cx
-      .try_catch(|cx| reflect_construct(cx, ctor, &[ss]))
-      .ok()
-      .flatten();
+  let out = cx
+      .try_catch(|cx| reflect_construct(cx, ctor, &[ss]))
+      .ok();

-  let out = cx
-      .try_catch(|cx| reflect_construct(cx, ctor, &[arg]))
-      .ok()
-      .flatten();
+  let out = cx
+      .try_catch(|cx| reflect_construct(cx, ctor, &[arg]))
+      .ok();